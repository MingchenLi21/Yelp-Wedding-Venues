import Alert from 'react-bootstrap/Alert'

const LogoutFirst = ()=>{
    return (
        <Alert variant="warning">
            Please logout First!
        </Alert>
    );
};

export default LogoutFirst;